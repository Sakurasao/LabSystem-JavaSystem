Taruh file dependensi JAR Anda di folder ini (misal: mariadb-java-client.jar, jakarta.servlet-api-6.0.0.jar).

File JAR yang ada di sini akan otomatis dimuat oleh Apache Tomcat saat aplikasi dijalankan, dan digunakan untuk proses kompilasi Java.
